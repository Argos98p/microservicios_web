
server:
  port: 8080
spring:
  cloud:
    config:
      server:
        git:
          default-label: master
          uri: https://github.com/Argos98p/microservicios_web.git
          searchPaths: config-data
          username: ${GIT_USER}
          password: ${GIT_PASSWORD}

  security:
    user:
      name: root
      password: s3cr3t
